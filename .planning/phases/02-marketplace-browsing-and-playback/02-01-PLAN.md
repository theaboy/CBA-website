---
phase: 02-marketplace-browsing-and-playback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/beats/*, public/audio/*, public/cba/*, app/(marketing)/beats/page.tsx, lib/site.ts]
autonomous: true
requirements: [BEAT-01, BEAT-02]
user_setup: []
must_haves:
  truths:
    - "The app has a stable placeholder beat dataset that can drive cards, filters, homepage features, and the mini-player."
    - "Beat metadata is normalized and includes the fields needed for browse UI and playback."
    - "Placeholder audio and artwork assets are available for the marketplace experience."
  artifacts:
    - path: "lib/beats/*"
      provides: "Beat types, placeholder catalog, and display helpers"
    - path: "public/audio/*"
      provides: "Playable placeholder audio assets"
    - path: "public/cba/*"
      provides: "Artwork or image sources used by beat cards/features"
  key_links:
    - "Beat pages and homepage modules both consume the same beat dataset"
    - "Playback logic can read directly from the normalized beat model"
---

<objective>
Create the beat data foundation for the marketplace and homepage feature surfaces.

Purpose: Ensure every later marketplace component shares one catalog model rather than inventing local placeholder data.
Output: Beat types, placeholder beat dataset, asset references, and helper utilities for formatting/filtering inputs.
</objective>

<execution_context>
@/Users/bird/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bird/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketplace-browsing-and-playback/02-CONTEXT.md
@.planning/phases/02-marketplace-browsing-and-playback/02-RESEARCH.md
@lib/site.ts
@app/(marketing)/beats/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the normalized beat model and placeholder catalog</name>
  <files>lib/beats/*, lib/site.ts</files>
  <action>Create a dedicated beat domain module with types and a curated placeholder catalog. Include id, slug, title, bpm, genre, mood, price, duration, artwork source, audio source, and any display flags such as featured/latest/popular. Avoid scattering raw beat objects across components.</action>
  <verify>TypeScript can import the beat model from multiple components without local type duplication.</verify>
  <done>A single source of truth exists for beat metadata and display-ready placeholder content.</done>
</task>

<task type="auto">
  <name>Task 2: Add placeholder audio and artwork assets for real playback testing</name>
  <files>public/audio/*, public/cba/*, lib/beats/*</files>
  <action>Add a small set of local placeholder audio and artwork references so the marketplace and mini-player can use actual media. Keep assets lightweight and consistent with the premium visual direction.</action>
  <verify>Each placeholder beat resolves to a valid local media path for audio and artwork.</verify>
  <done>The catalog can be rendered and played without missing local assets.</done>
</task>

<task type="auto">
  <name>Task 3: Add catalog helper utilities for filter and display logic</name>
  <files>lib/beats/*</files>
  <action>Create helper functions and constants for unique genres, moods, BPM limits, price ranges, and featured selections. This should prepare the browse UI and homepage integration without hardcoding filter lists in component files.</action>
  <verify>Filters and featured sections can derive their inputs from reusable helpers instead of duplicated arrays.</verify>
  <done>The beat catalog exposes reusable helpers for the rest of Phase 2.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Placeholder catalog entries resolve to valid local media references
- [ ] Beat model and helpers are reusable from both listing and homepage contexts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Beat metadata and placeholder media are ready for browse and playback work
  </success_criteria>

<output>
After completion, create `.planning/phases/02-marketplace-browsing-and-playback/02-01-SUMMARY.md`
</output>
