---
phase: 02-marketplace-browsing-and-playback
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [components/audio/*, lib/audio/*, app/layout.tsx, app/globals.css]
autonomous: true
requirements: [BEAT-03, BEAT-04]
user_setup: []
must_haves:
  truths:
    - "Only one beat can play at a time across the public experience."
    - "A persistent mini-player remains available while navigating public pages."
    - "Cards and featured sections use the same centralized playback controls."
  artifacts:
    - path: "lib/audio/*"
      provides: "Central playback provider or store"
    - path: "components/audio/*"
      provides: "Mini-player and reusable play-control components"
    - path: "app/layout.tsx"
      provides: "Playback provider mounting point across route transitions"
  key_links:
    - "Public pages are wrapped in the playback provider so route changes retain current track state"
    - "Beat card controls and mini-player both operate on the same shared playback state"
---

<objective>
Implement the centralized playback system and persistent mini-player.

Purpose: Make one-track-at-a-time behavior and route-persistent playback reliable before deeper marketplace UI lands.
Output: Playback provider/store, reusable play controls, and a mini-player mounted across the public experience.
</objective>

<execution_context>
@/Users/bird/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bird/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketplace-browsing-and-playback/02-CONTEXT.md
@.planning/phases/02-marketplace-browsing-and-playback/02-RESEARCH.md
@app/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized playback state and provider boundaries</name>
  <files>lib/audio/*, app/layout.tsx</files>
  <action>Implement the shared playback state for current beat, play/pause, progress, and route persistence. Mount the provider high enough that navigating between public pages does not reset the current track. Keep the logic isolated from UI components.</action>
  <verify>Playback state is stored centrally and exposed through reusable hooks or context instead of being owned by individual cards.</verify>
  <done>A single playback source of truth exists across the app shell.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable play controls and the persistent mini-player</name>
  <files>components/audio/*, app/globals.css</files>
  <action>Create the mini-player UI plus shared play controls that beat cards and featured surfaces can reuse. The mini-player should feel visually integrated with the premium dark system rather than like a generic fixed footer utility.</action>
  <verify>The mini-player can render current track metadata and controls using the shared playback state.</verify>
  <done>Playback controls and persistent player UI are available to the marketplace and homepage.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce one-track-at-a-time behavior with real placeholder audio</name>
  <files>lib/audio/*, components/audio/*</files>
  <action>Wire the playback system against the placeholder audio catalog from Plan 01 and ensure starting a new track stops the previous one. Handle common transitions such as pause, track switching, and navigation between pages.</action>
  <verify>Starting a different beat stops the previous audio source and updates the mini-player correctly.</verify>
  <done>One-track playback is reliable and shared across the public experience.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] One-track-at-a-time playback behavior works with placeholder audio
- [ ] Mini-player persists across navigation and reflects current track state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has a dependable playback foundation for marketplace UI
  </success_criteria>

<output>
After completion, create `.planning/phases/02-marketplace-browsing-and-playback/02-02-SUMMARY.md`
</output>
