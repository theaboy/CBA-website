---
phase: 03-beat-detail-and-inquiry-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/(marketing)/beats/[slug]/page.tsx, components/beats/*, lib/beats/*, lib/audio/*]
autonomous: true
requirements: [BEAT-05]
user_setup: []
must_haves:
  truths:
    - "Each beat can be opened on a dedicated public detail route using the shared catalog slug."
    - "The detail page presents larger playback, richer metadata, descriptive copy, and a waveform-ready visual surface."
    - "The detail route stays synchronized with the shared one-track-at-a-time playback system and mini-player."
  artifacts:
    - path: "app/(marketing)/beats/[slug]/page.tsx"
      provides: "Dedicated beat detail route wired to the shared catalog"
    - path: "components/beats/*"
      provides: "Beat detail presentation modules including a waveform-ready media area"
    - path: "lib/beats/*"
      provides: "Catalog helpers for slug lookup and detail-page metadata"
  key_links:
    - "Beat cards and featured modules can deep-link to the corresponding beat detail route"
    - "Detail-page playback controls use the same shared audio state as the browse experience"
---

<objective>
Create the dedicated beat detail experience that turns marketplace browsing into a deeper product view.

Purpose: Give each beat a shareable, premium public page that expands playback and metadata without breaking the Phase 2 audio model.
Output: Dynamic beat detail route, large player composition, waveform-ready presentation module, and richer beat metadata display.
</objective>

<execution_context>
@/Users/bird/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bird/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-beat-detail-and-inquiry-conversion/03-CONTEXT.md
@.planning/phases/03-beat-detail-and-inquiry-conversion/03-RESEARCH.md
@lib/beats/catalog.ts
@lib/audio/audio-context.tsx
@components/beats/beat-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared beat lookup and route helpers</name>
  <files>lib/beats/*</files>
  <action>Extend the beat domain module with stable slug lookup, detail-page metadata helpers, and any richer placeholder fields needed for description, tags, and license anchors. Keep the catalog as the single source of truth.</action>
  <verify>Beat detail routes can resolve a beat deterministically from the shared catalog without duplicating data in the page file.</verify>
  <done>The beat model supports both marketplace cards and dedicated detail pages.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dynamic beat detail route and premium presentation</name>
  <files>app/(marketing)/beats/[slug]/page.tsx, components/beats/*</files>
  <action>Create a dedicated beat page with large artwork/player treatment, denser metadata, description, and a waveform-ready visual module. The page should feel more premium and focused than the marketplace grid while preserving the established brand system.</action>
  <verify>Opening a known beat slug renders a complete detail page; invalid slugs fail cleanly using the app's routing conventions.</verify>
  <done>Visitors can open a real beat detail page that feels purpose-built for conversion.</done>
</task>

<task type="auto">
  <name>Task 3: Reuse shared playback controls on the detail surface</name>
  <files>components/beats/*, lib/audio/*, app/(marketing)/beats/[slug]/page.tsx</files>
  <action>Integrate the existing playback hooks so the detail page can start and control the current beat without introducing separate audio state. Make sure the mini-player and detail controls stay in sync.</action>
  <verify>Starting playback from the detail page updates the shared mini-player and still enforces one-track-at-a-time behavior.</verify>
  <done>The detail page participates in the same audio system as the marketplace and homepage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Known beat slugs open valid detail pages and invalid slugs fail cleanly
- [ ] Detail-page playback remains synchronized with the shared mini-player
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Beat detail pages feel premium, shareable, and playback-consistent
  </success_criteria>

<output>
After completion, create `.planning/phases/03-beat-detail-and-inquiry-conversion/03-01-SUMMARY.md`
</output>
