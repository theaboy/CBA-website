---
phase: 01-foundation-and-brand-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [app/(marketing)/**, components/site-header.*, components/site-footer.*, components/navigation/*, lib/site.ts]
autonomous: true
requirements: [FND-01, FND-03]
user_setup: []
must_haves:
  truths:
    - "Visitors can navigate all primary public routes from a shared public shell."
    - "Marketing pages use a consistent responsive header, footer, and layout structure."
    - "Primary public navigation is data-driven and easy to extend."
  artifacts:
    - path: "app/(marketing)/layout.tsx"
      provides: "Shared public marketing layout"
    - path: "app/(marketing)/*/page.tsx"
      provides: "Scaffolded public routes for all primary pages"
    - path: "components/site-header.*"
      provides: "Responsive public navigation shell"
    - path: "components/site-footer.*"
      provides: "Shared footer and supporting navigation"
  key_links:
    - "Public route files render through the shared marketing layout"
    - "Navigation configuration in lib/site.ts drives the public shell"
---

<objective>
Create the public route architecture and shared navigation shell for the full website.

Purpose: Ensure every major public destination exists and shares a coherent responsive structure before feature-specific content is built.
Output: Public route group, route files, shared header/footer, and a reusable marketing layout.
</objective>

<execution_context>
@/Users/bird/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bird/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-brand-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-brand-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the marketing route group and shared shell</name>
  <files>app/(marketing)/layout.tsx, components/site-header.*, components/site-footer.*, components/navigation/*</files>
  <action>Create the public route-group layout with a responsive header and footer. The shell should feel premium and intentionally structured for future beats, studio, events, and brand pages, not like placeholder boilerplate.</action>
  <verify>Public routes render through a common layout and navigation adapts cleanly across mobile and desktop sizes.</verify>
  <done>A shared marketing shell exists and is reused by the primary public pages.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold all required public routes with route-specific placeholders</name>
  <files>app/(marketing)/page.tsx, app/(marketing)/beats/page.tsx, app/(marketing)/events/page.tsx, app/(marketing)/studio/page.tsx, app/(marketing)/dj-services/page.tsx, app/(marketing)/about/page.tsx, app/(marketing)/contact/page.tsx</files>
  <action>Create route files for Home, Beats, Events, Studio, DJ Services, About, and Contact. Each page should render meaningful structured placeholder sections that match the route purpose and preserve the premium tone, while avoiding deep feature implementation that belongs to later phases.</action>
  <verify>Each primary route loads without errors and presents route-specific structure rather than empty stubs.</verify>
  <done>All roadmap-required public pages exist and are reachable through the shared navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Centralize navigation and metadata configuration</name>
  <files>lib/site.ts, app/(marketing)/layout.tsx, components/navigation/*</files>
  <action>Move public nav labels, route definitions, and shared site metadata into configuration so later plans can extend or restyle the shell without editing multiple page files.</action>
  <verify>Header/footer navigation and route metadata are driven from shared configuration.</verify>
  <done>The public shell is configuration-backed and ready for future feature pages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Every required public route renders through the marketing shell
- [ ] Header and footer work on mobile and desktop layouts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Primary public route architecture is complete and reusable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-brand-system/01-02-SUMMARY.md`
</output>
