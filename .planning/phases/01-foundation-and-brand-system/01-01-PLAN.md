---
phase: 01-foundation-and-brand-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.*, tsconfig.json, app/layout.tsx, app/globals.css, lib/site.ts, components/layout/*]
autonomous: true
requirements: [FND-01, FND-02, FND-03]
user_setup: []
must_haves:
  truths:
    - "The project runs as a Next.js application with a public and admin route foundation."
    - "Global theme tokens establish a premium dark visual baseline."
    - "Shared layout primitives exist for later phases to reuse."
  artifacts:
    - path: "package.json"
      provides: "App runtime, scripts, and dependency baseline"
    - path: "app/layout.tsx"
      provides: "Root layout and app-wide providers"
    - path: "app/globals.css"
      provides: "Global theme tokens and foundational styling"
    - path: "lib/site.ts"
      provides: "Shared site metadata and navigation configuration"
  key_links:
    - "app/layout.tsx imports app/globals.css and shared site configuration"
    - "Route groups created later can rely on the root shell and token system defined here"
---

<objective>
Bootstrap the product as a durable Next.js app foundation rather than a throwaway landing page.

Purpose: Create the technical and stylistic base that every later phase will extend.
Output: App runtime, root layout, global theme tokens, and shared site configuration primitives.
</objective>

<execution_context>
@/Users/bird/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bird/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-brand-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-brand-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Next.js application and core tooling</name>
  <files>package.json, next.config.*, tsconfig.json, app/layout.tsx, app/page.tsx</files>
  <action>Create the initial Next.js App Router application structure in the existing repo, including scripts, TypeScript configuration, and a root layout. Keep the scaffold intentionally lean and aligned to future public/admin route groups rather than generating unused boilerplate.</action>
  <verify>App dependencies install cleanly and the project can run/build with standard Next.js scripts.</verify>
  <done>A working app scaffold exists with a root layout and initial route entry point.</done>
</task>

<task type="auto">
  <name>Task 2: Encode the brand system as global design tokens and shared primitives</name>
  <files>app/globals.css, components/layout/*, lib/site.ts</files>
  <action>Define CSS variables and base typography, spacing, radius, color, and surface tokens that express the dark premium identity. Build shared layout primitives for containers, section spacing, and metadata-driven navigation configuration so later pages do not duplicate structure.</action>
  <verify>Global CSS loads through the root layout and the initial page reflects the new token system instead of default framework styling.</verify>
  <done>The app has a reusable design-system baseline and central site configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Capture foundational technical decisions in the codebase structure</name>
  <files>README.md, lib/site.ts, app/layout.tsx</files>
  <action>Document the chosen app structure briefly where useful, especially the intended public/admin split and the role of shared site configuration. Keep comments and docs minimal but enough to prevent later structural drift.</action>
  <verify>Core files make the public/admin separation and shared configuration discoverable to future plans.</verify>
  <done>The app structure communicates its intended extension points for later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Root layout loads global styling and metadata without errors
- [ ] Shared site configuration is used by the app shell
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The project has a credible technical and visual foundation for subsequent feature phases
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-brand-system/01-01-SUMMARY.md`
</output>
