---
phase: 01-foundation-and-brand-system
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [app/(admin)/**, components/admin/*, middleware.ts, lib/auth/*]
autonomous: true
requirements: [FND-01]
user_setup: []
must_haves:
  truths:
    - "An internal admin area exists as a separate protected shell."
    - "Admin navigation and layout are structurally ready for future management tools."
    - "The admin surface is isolated from the public marketing shell."
  artifacts:
    - path: "app/(admin)/layout.tsx"
      provides: "Admin shell and layout boundary"
    - path: "app/(admin)/admin/page.tsx"
      provides: "Initial dashboard landing page"
    - path: "components/admin/*"
      provides: "Reusable admin layout/navigation components"
    - path: "middleware.ts"
      provides: "Protection or route-guard entry point for internal routes"
  key_links:
    - "Admin routes render through their own layout rather than the public marketing shell"
    - "Route protection hooks into admin path handling for future auth expansion"
---

<objective>
Scaffold the protected admin area so later operations work lands on a real internal foundation.

Purpose: Prevent the admin dashboard from becoming an afterthought bolted onto a public-only site.
Output: Admin route group, internal layout, route protection entry point, and dashboard shell components.
</objective>

<execution_context>
@/Users/bird/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bird/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-brand-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-brand-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the admin route group and internal shell</name>
  <files>app/(admin)/layout.tsx, app/(admin)/admin/page.tsx, components/admin/*</files>
  <action>Build a separate admin route-group layout with internal navigation, page chrome, and an overview dashboard shell. Keep the interface simpler than the public site but still intentional, clear, and production-oriented.</action>
  <verify>The `/admin` experience renders through a dedicated internal layout with reusable admin components.</verify>
  <done>A real admin shell exists independent of the public marketing shell.</done>
</task>

<task type="auto">
  <name>Task 2: Establish route protection and future-auth boundaries</name>
  <files>middleware.ts, lib/auth/*, app/(admin)/layout.tsx</files>
  <action>Create the initial route-protection strategy for admin paths. It can use placeholder or lightweight gating if full auth is not yet in place, but the code should clearly mark the seam where real admin authentication will be connected later.</action>
  <verify>Admin route handling includes protection logic or an explicit gating boundary rather than being fully public.</verify>
  <done>The admin shell is structurally prepared for real authorization work in a later phase.</done>
</task>

<task type="auto">
  <name>Task 3: Add placeholder operational modules that anticipate Phase 8</name>
  <files>app/(admin)/admin/page.tsx, components/admin/*</files>
  <action>Populate the dashboard shell with clear placeholders or summary cards for homepage content, events, studio availability, bookings, DJ inquiries, and beats metadata. These should communicate the intended admin IA without prematurely implementing CRUD.</action>
  <verify>The admin landing page previews the future management areas CBA will use.</verify>
  <done>The admin shell already points toward the operational modules planned in later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Admin routes use a dedicated internal layout
- [ ] Admin path handling is not exposed as an unstructured public page
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The project has a credible admin foundation for later operational work
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-brand-system/01-04-SUMMARY.md`
</output>
