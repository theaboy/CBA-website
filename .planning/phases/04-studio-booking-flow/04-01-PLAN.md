---
phase: 04-studio-booking-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/studio/*, lib/inquiries/*, lib/site.ts]
autonomous: true
requirements: [STUD-01, STUD-02, STUD-03]
user_setup: []
must_haves:
  truths:
    - "Studio packages, pricing anchors, and available slots are modeled as structured local data."
    - "Booking requests have a normalized payload shape ready for server-side handling."
    - "The Studio page and booking flow can derive their content from shared domain helpers instead of hardcoded JSX."
  artifacts:
    - path: "lib/studio/*"
      provides: "Studio package definitions, availability model, and display helpers"
    - path: "lib/inquiries/*"
      provides: "Reusable studio booking request schema or validation helpers"
    - path: "lib/site.ts"
      provides: "Updated Studio page summary copy if needed to align with the real booking phase"
  key_links:
    - "Studio marketing and booking UI both consume the same package and availability data"
    - "Booking submission handling can normalize requests from structured slot and package selections"
---

<objective>
Create the studio domain foundation for packages, availability, and booking requests.

Purpose: Ensure the Studio page and booking flow share one data model instead of inventing placeholder content independently.
Output: Studio package data, availability data, helper utilities, and booking request schema/validation foundations.
</objective>

<execution_context>
@/Users/bird/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bird/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-studio-booking-flow/04-CONTEXT.md
@.planning/phases/04-studio-booking-flow/04-RESEARCH.md
@app/(marketing)/studio/page.tsx
@lib/inquiries/beat-inquiry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define studio packages and pricing anchors</name>
  <files>lib/studio/*</files>
  <action>Create a studio domain module with package definitions, durations, price anchors, descriptive copy, and any metadata needed for marketing and booking surfaces. Keep package data structured and reusable.</action>
  <verify>Studio packages can be rendered from shared data without duplicating pricing or copy in page components.</verify>
  <done>The app has a stable package model for the Studio experience.</done>
</task>

<task type="auto">
  <name>Task 2: Define availability and slot selection data</name>
  <files>lib/studio/*</files>
  <action>Add a local availability model for bookable dates and time slots, with enough structure to support a real-looking browse UI and future admin control. Include helpers for grouping or filtering slots by day or package if useful.</action>
  <verify>The UI can derive available dates and slots from shared helpers instead of hardcoded component arrays.</verify>
  <done>The Studio booking flow has a reusable availability data source.</done>
</task>

<task type="auto">
  <name>Task 3: Define the studio booking request schema boundary</name>
  <files>lib/inquiries/*, lib/studio/*</files>
  <action>Create the normalized payload shape and validation strategy for studio booking requests, following the same architectural pattern introduced for beat inquiries while keeping studio-specific fields explicit.</action>
  <verify>Booking submissions can be validated and normalized using shared modules before UI wiring begins.</verify>
  <done>The Studio flow has a server-ready request contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` succeeds
- [ ] Studio packages and slots are derived from shared domain modules
- [ ] Booking request schema covers package, date, time, duration, contact details, and notes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Studio booking work has a clean domain foundation for later plans
  </success_criteria>

<output>
After completion, create `.planning/phases/04-studio-booking-flow/04-01-SUMMARY.md`
</output>
