---
phase: 05-payments-email-ticketing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/stripe-webhook.test.ts
  - tests/create-payment-intent.test.ts
  - tests/ticket-purchase.test.ts
  - tests/email-send.test.ts
  - tests/events-queries.test.ts
  - tests/download-route.test.ts
  - lib/stripe/client.ts
  - lib/stripe/server.ts
  - app/api/stripe/create-payment-intent/route.ts
  - app/api/stripe/webhook/route.ts
  - app/api/download/[token]/route.ts
autonomous: true
requirements:
  - BE-05
must_haves:
  truths:
    - "POST /api/stripe/create-payment-intent returns clientSecret for valid beats"
    - "POST /api/stripe/create-payment-intent returns 409 when exclusive beat is already sold"
    - "Webhook handler fulfills beat purchase (inserts orders_beat row) on payment_intent.succeeded"
    - "Webhook handler is idempotent — duplicate stripe_payment_intent_id skips second insert"
    - "Exclusive license sets is_exclusive_sold = true atomically via UPDATE WHERE"
    - "GET /api/download/[token] redirects to signed URL for valid token"
  artifacts:
    - path: "lib/stripe/server.ts"
      provides: "Shared Stripe SDK singleton (secret key)"
      exports: ["stripe"]
    - path: "lib/stripe/client.ts"
      provides: "loadStripe promise for client-side use"
      exports: ["stripePromise"]
    - path: "app/api/stripe/create-payment-intent/route.ts"
      provides: "POST endpoint returning clientSecret"
      exports: ["POST"]
    - path: "app/api/stripe/webhook/route.ts"
      provides: "Stripe webhook handler — beat + ticket fulfillment"
      exports: ["POST"]
    - path: "app/api/download/[token]/route.ts"
      provides: "Download token validation + signed URL redirect"
      exports: ["GET"]
    - path: "tests/stripe-webhook.test.ts"
      provides: "Webhook fulfillment tests (BE-05)"
    - path: "tests/create-payment-intent.test.ts"
      provides: "PaymentIntent creation tests (BE-05)"
    - path: "tests/download-route.test.ts"
      provides: "Download token route tests (BE-05)"
  key_links:
    - from: "app/api/stripe/create-payment-intent/route.ts"
      to: "lib/stripe/server.ts"
      via: "import stripe singleton"
      pattern: "import.*stripe.*lib/stripe/server"
    - from: "app/api/stripe/webhook/route.ts"
      to: "lib/supabase/service.ts"
      via: "createServiceClient() for DB writes"
      pattern: "createServiceClient"
    - from: "app/api/stripe/webhook/route.ts"
      to: "lib/email/send.ts"
      via: "sendBeatPurchaseEmail() after fulfillment"
      pattern: "sendBeatPurchaseEmail"
user_setup:
  - service: stripe
    why: "Beat and ticket payment processing"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key (use sk_test_ for development)"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Run `stripe listen --forward-to localhost:3000/api/stripe/webhook` — prints the webhook signing secret"
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Publishable key"
    dashboard_config:
      - task: "Install Stripe CLI for local webhook forwarding"
        location: "https://docs.stripe.com/stripe-cli"
---

<objective>
Install Stripe npm packages, create Wave 0 test stubs for all six Phase 5 test files, implement the Stripe server/client singletons, the create-payment-intent API route, the webhook handler (beat fulfillment path only), and the download token route.

Purpose: Establishes the complete payment infrastructure for beat purchases — the foundational API layer that Plan 04 (frontend) and Plan 03 (email) connect to.
Output: lib/stripe/{server,client}.ts, three API routes, and six test stub files that all pass (it.todo() stubs).
</objective>

<execution_context>
@/Users/bird/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bird/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bird/CBA-website/.planning/PROJECT.md
@/Users/bird/CBA-website/.planning/ROADMAP.md
@/Users/bird/CBA-website/.planning/phases/05-payments-email-ticketing/05-RESEARCH.md

<interfaces>
<!-- Three-client pattern from Phase 4 — use these exact import paths -->

From lib/supabase/service.ts:
```typescript
export function createServiceClient(): SupabaseClient
// Uses SUPABASE_SERVICE_ROLE_KEY — for admin writes only, bypasses RLS
```

From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
// Uses anon key with cookie-based session — for server reads
```

From tests/beats-queries.test.ts (mock pattern to replicate):
```typescript
// vi.mock() at module level
vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn().mockResolvedValue({ from: mockFrom }),
}))
// it.todo() for Wave 0 stubs
it.todo('describe what this test will verify')
```

Schema — orders_beat columns:
  id uuid, beat_id uuid, license_type text, stripe_payment_intent_id text,
  buyer_email text, buyer_name text, amount_paid numeric,
  full_download_token uuid default gen_random_uuid(), download_count int default 0

Schema — beats relevant columns:
  id uuid, title text, price_basic numeric, price_premium numeric,
  price_exclusive numeric, is_exclusive_sold boolean, published boolean, full_key text
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create all six test stub files</name>
  <files>
    tests/stripe-webhook.test.ts,
    tests/create-payment-intent.test.ts,
    tests/ticket-purchase.test.ts,
    tests/email-send.test.ts,
    tests/events-queries.test.ts,
    tests/download-route.test.ts
  </files>
  <behavior>
    All six files must exist and pass `npm test` using `it.todo()` stubs.
    Mocks are declared at module level with `vi.mock()` (same pattern as tests/beats-queries.test.ts).

    stripe-webhook.test.ts stubs:
    - "fulfillBeatPurchase inserts an orders_beat row on payment_intent.succeeded"
    - "fulfillBeatPurchase sets is_exclusive_sold = true for exclusive license"
    - "fulfillBeatPurchase is idempotent — duplicate stripe_payment_intent_id skips second insert"
    - "fulfillBeatPurchase calls sendBeatPurchaseEmail with correct args"

    create-payment-intent.test.ts stubs:
    - "returns clientSecret for a valid beat and license type"
    - "returns 409 when exclusive beat has is_exclusive_sold = true"
    - "returns 404 when beat does not exist or is not published"

    ticket-purchase.test.ts stubs:
    - "fulfillTicketPurchase inserts an orders_ticket row"
    - "fulfillTicketPurchase calls purchase_ticket RPC with correct args"
    - "purchase_ticket RPC raises SOLD_OUT when quantity exceeds capacity"
    - "qr_token in tickets table is a valid UUID v4 format"

    email-send.test.ts stubs:
    - "sendBeatPurchaseEmail calls resend.emails.send with correct from/to/subject"
    - "sendTicketPurchaseEmail calls resend.emails.send with correct from/to/subject"

    events-queries.test.ts stubs:
    - "getPublishedEvents returns events array without error"
    - "getPublishedEvents filters to published = true only"
    - "getPublishedEvents orders results by date ascending"

    download-route.test.ts stubs:
    - "GET /api/download/[token] redirects to signed URL for valid token"
    - "GET /api/download/[token] returns 404 for unknown token"
    - "GET /api/download/[token] increments download_count"

    Mock declarations needed in each file:
    - stripe-webhook.test.ts: vi.mock('@/lib/supabase/service'), vi.mock('@/lib/email/send')
    - create-payment-intent.test.ts: vi.mock('@/lib/supabase/service'), vi.mock('@/lib/stripe/server')
    - ticket-purchase.test.ts: vi.mock('@/lib/supabase/service')
    - email-send.test.ts: vi.mock('resend')
    - events-queries.test.ts: vi.mock('@/lib/supabase/server')
    - download-route.test.ts: vi.mock('@/lib/supabase/service')
  </behavior>
  <action>
    Install Phase 5 npm packages first:
    `npm install stripe @stripe/stripe-js @stripe/react-stripe-js resend @react-email/components qrcode @types/qrcode`

    Then create all six test files. Each file follows the beats-queries.test.ts pattern exactly:
    1. Import { describe, it, vi } from 'vitest'
    2. Set up mock functions for the query builder chain (select, eq, order, single, maybeSingle, rpc, update)
    3. Call vi.mock() at module level for the relevant lib modules
    4. Write describe() block with all stubs as it.todo('description') — NO implementation yet
    5. Do NOT add beforeEach resets yet — Wave 0 stubs are contracts only

    Mocking resend in email-send.test.ts:
    ```typescript
    vi.mock('resend', () => ({
      Resend: vi.fn().mockImplementation(() => ({
        emails: { send: vi.fn().mockResolvedValue({ data: { id: 'mock-id' }, error: null }) },
      })),
    }))
    ```

    Mocking stripe in create-payment-intent.test.ts:
    ```typescript
    vi.mock('@/lib/stripe/server', () => ({
      stripe: {
        paymentIntents: {
          create: vi.fn().mockResolvedValue({ client_secret: 'pi_test_secret' }),
        },
      },
    }))
    ```
  </action>
  <verify>
    <automated>cd /Users/bird/CBA-website && npm test -- tests/stripe-webhook.test.ts tests/create-payment-intent.test.ts tests/ticket-purchase.test.ts tests/email-send.test.ts tests/events-queries.test.ts tests/download-route.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All six test files exist. `npm test` passes (all stubs show as "todo", zero failures).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stripe singletons, create-payment-intent route, webhook handler (beat path), download route</name>
  <files>
    lib/stripe/client.ts,
    lib/stripe/server.ts,
    app/api/stripe/create-payment-intent/route.ts,
    app/api/stripe/webhook/route.ts,
    app/api/download/[token]/route.ts
  </files>
  <behavior>
    create-payment-intent tests (from stubs):
    - Valid beat + license → 200 with { clientSecret }
    - Exclusive beat with is_exclusive_sold = true → 409 { error: 'Exclusive license already sold' }
    - Missing/unpublished beat → 404 { error: 'Beat not found' }

    stripe-webhook tests (from stubs):
    - payment_intent.succeeded with type=beat_purchase → orders_beat row inserted
    - payment_intent.succeeded with license_type=exclusive → is_exclusive_sold updated to true
    - Duplicate stripe_payment_intent_id → early return (maybeSingle returns existing row)
    - After fulfillment → sendBeatPurchaseEmail called with { to, buyerName, beatTitle, licenseType, downloadToken }

    download-route tests (from stubs):
    - Valid token → redirects to signed URL, download_count incremented
    - Invalid token → 404
  </behavior>
  <action>
    **lib/stripe/server.ts:**
    ```typescript
    import Stripe from 'stripe'
    export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
      apiVersion: '2025-12-15.acacia',
    })
    ```

    **lib/stripe/client.ts:**
    ```typescript
    import { loadStripe } from '@stripe/stripe-js'
    export const stripePromise = loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!)
    ```

    **app/api/stripe/create-payment-intent/route.ts:**
    - POST handler — accepts { beatId, licenseType, buyerEmail, buyerName }
    - Uses createServiceClient() to query beats table with columns: id, title, price_basic, price_premium, price_exclusive, is_exclusive_sold; filters .eq('published', true)
    - Returns 404 if beat not found; 409 if licenseType === 'exclusive' && beat.is_exclusive_sold
    - Price map: { basic: price_basic, premium: price_premium, exclusive: price_exclusive }; amount = Math.round(price * 100) (cents)
    - Creates PaymentIntent with currency 'cad', automatic_payment_methods: { enabled: true }
    - Metadata: { type: 'beat_purchase', beat_id, beat_title: beat.title, license_type, buyer_email, buyer_name }
    - Returns { clientSecret: paymentIntent.client_secret }

    **app/api/stripe/webhook/route.ts:**
    - POST handler — MUST use `const body = await req.text()` as first line (NOT req.json())
    - Import headers from 'next/headers'; `const sig = (await headers()).get('stripe-signature')!`
    - Verify: `stripe.webhooks.constructEvent(body, sig, process.env.STRIPE_WEBHOOK_SECRET!)`
    - Return 400 on verification failure
    - Only handle 'payment_intent.succeeded' event
    - Dispatch to fulfillBeatPurchase() if pi.metadata.type === 'beat_purchase'
    - Dispatch to fulfillTicketPurchase() if pi.metadata.type === 'ticket_purchase' (stub this function — Plan 02 implements it)
    - Always return { received: true } 200

    fulfillBeatPurchase(supabase, pi):
    1. Idempotency: query orders_beat by stripe_payment_intent_id (.maybeSingle()); return if exists
    2. Insert orders_beat: { beat_id, license_type, stripe_payment_intent_id, buyer_email, buyer_name, amount_paid: pi.amount / 100 }
    3. Select id + full_download_token from the inserted row
    4. If license_type === 'exclusive': .update({ is_exclusive_sold: true }).eq('id', beat_id).eq('is_exclusive_sold', false)
    5. Call sendBeatPurchaseEmail({ to: buyer_email, buyerName: buyer_name, beatTitle: beat_title, licenseType: license_type, downloadToken: order.full_download_token })

    fulfillTicketPurchase() — export empty async stub that logs 'ticket fulfillment pending Plan 02'

    **app/api/download/[token]/route.ts:**
    - GET handler — params: Promise<{ token: string }>; `const { token } = await params`
    - Query orders_beat: .select('beat_id, full_download_token, download_count').eq('full_download_token', token).single()
    - Return 404 if not found
    - Query beats using service client: .select('full_key').eq('id', order.beat_id).single()
    - Return 404 if no full_key
    - Generate signed URL: supabase.storage.from('full-audio').createSignedUrl(beat.full_key, 3600)
    - Increment download_count: .update({ download_count: order.download_count + 1 }).eq('full_download_token', token)
    - Return NextResponse.redirect(urlData.signedUrl)

    Convert it.todo() stubs to real tests as each function is implemented. Mock setup:
    - vi.mock('@/lib/stripe/server') returns { stripe: { webhooks: { constructEvent: vi.fn() }, paymentIntents: { create: vi.fn() } } }
    - vi.mock('@/lib/supabase/service') returns { createServiceClient: vi.fn().mockReturnValue({ from: mockFrom }) }
    - vi.mock('@/lib/email/send') returns { sendBeatPurchaseEmail: vi.fn() }
  </action>
  <verify>
    <automated>cd /Users/bird/CBA-website && npm test -- tests/stripe-webhook.test.ts tests/create-payment-intent.test.ts tests/download-route.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - lib/stripe/server.ts and client.ts exist with correct exports
    - All three API routes exist under app/api/stripe/ and app/api/download/
    - stripe-webhook.test.ts: beat fulfillment, idempotency, exclusive lock, email call tests pass
    - create-payment-intent.test.ts: valid/exclusive-sold/not-found tests pass
    - download-route.test.ts: valid token redirect, 404, download_count increment tests pass
  </done>
</task>

</tasks>

<verification>
Full suite passes: `cd /Users/bird/CBA-website && npm test`

API routes are importable without runtime errors (TypeScript build check):
`cd /Users/bird/CBA-website && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10`
</verification>

<success_criteria>
- npm install succeeded — stripe, @stripe/stripe-js, @stripe/react-stripe-js, resend, @react-email/components, qrcode in package.json
- All six test stub files exist in tests/
- lib/stripe/server.ts exports `stripe` singleton
- lib/stripe/client.ts exports `stripePromise`
- app/api/stripe/create-payment-intent/route.ts: POST returns clientSecret; 409 on exclusive sold
- app/api/stripe/webhook/route.ts: uses req.text(), verifies signature, fulfills beat purchase idempotently
- app/api/download/[token]/route.ts: validates token, returns signed URL redirect
- `npm test` passes with zero failures
</success_criteria>

<output>
After completion, create `/Users/bird/CBA-website/.planning/phases/05-payments-email-ticketing/05-01-SUMMARY.md`
</output>
